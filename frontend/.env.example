# ---- Supabase (public values — safe for the browser) ----
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ---- Backend API ----
# Leave empty in local dev to use the Vite proxy (/api -> localhost:8000).
# In production set this to your deployed backend, e.g. https://api.example.com
VITE_API_BASE_URL=
